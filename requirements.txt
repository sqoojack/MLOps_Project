# Core data processing and models
pandas<2.2.0
numpy<2.0
scikit-learn
torch

# MLOps and version control (S3 support integration)
dvc[s3]==3.48.0
s3fs==2024.3.1
aiobotocore==2.12.1
mlflow

# AWS related (compatible with s3fs)
boto3==1.34.51
botocore==1.34.51
sagemaker

# API services
fastapi
uvicorn
joblib
prometheus-fastapi-instrumentator
python-dotenv

# Other dataset tools
redis
datasets==2.19.0
pyarrow

# ONNX stack (Synced for Python 3.10 and dependency resolution)
onnx>=1.16.0
onnxscript
onnxruntime-gpu>=1.18.0
tensorrt>=10.0.0